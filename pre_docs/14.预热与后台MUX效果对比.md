# 14. 预热 + 后台 MUX 效果对比

> **测试时间**：2026-08-22 14:05 - 16:07（UTC+8）
> **测试脚本**：`playground/bench_6cases_concurrent.py`
> **对比基线**：见《13.step10 并发测试报告》（commit `031ae64`）
> **本次改动**：commit `a358e96 feat: add startup model warmup and async mux background queue`
> **硬件**：2 × NVIDIA GeForce RTX 4090（49140 MiB）

---

## 14.1 改动回顾

`api_server.py` 中做了三处改动（commit `a358e96`）：

1. **启动预热**（`_warmup_all_workers / _warmup_one`）
   - FastAPI `startup()` 中后台并发提交 1 帧 1 step 的 dummy prompt 给两张卡
   - H3 模型（UNet + CLIP + VAE）在业务请求之前就被加载到显存
2. **后台 MUX 队列**（`_post_queue + _mux_worker_loop × 2`）
   - `/api/v1/task/{id}` 轮询路径不再 `await _post_process(...)`
   - 改为 `_post_queue.put_nowait(...)`，由 2 个后台协程消费
3. **`/health` 增加 warm 状态**
   - 返回 `warming` / `ok` / `degraded` 三态
   - 每个 worker 报告 `warmed: true/false`

---

## 14.2 测试对比

同一脚本（`bench_6cases_concurrent.py`）、同一参数（steps=10, duration=5）、同一机器、同一素材，仅 `api_server.py` 不同。

| 维度 | 第一次（cold + 阻塞 mux） | 第二次（warm + 后台 mux） | 提升 |
|------|---------------------------|---------------------------|------|
| **总耗时（端到端 6 case）** | 1344.5s（22.4 min） | **550.4s（9.2 min）** | **2.44×** |
| Case 1+2 单任务耗时 | 974 / 991s | **201 / 204s** | **4.8×** |
| Case 3+4 单任务耗时 | 1140 / 1140s | **346 / 349s** | **3.3×** |
| Case 5+6 单任务耗时 | 1296 / 1344s | 501 / 550s | **2.5×** |
| 单任务均耗时 | 448s | 326s | 1.37× |
| 实测吞吐 | 0.27 tasks/min | **0.65 tasks/min** | **2.41×** |
| 启动 → 可用 | ~30s（服务就绪即视为可用） | ~30s 服务就绪 + 7.5 min 模型加载 | N/A* |

> *见 14.4 节"启动时长"权衡说明。

### 14.2.1 各 case 详细耗时

| case | 名称 | worker | e2e (s) | 状态 |
|------|------|--------|---------|------|
| 1 | t2v 基础 | GPU0 | **201.6** | success |
| 2 | t2v + 静音 | GPU1 | **204.6** | success |
| 3 | t2i2v + 中文口播 | GPU0 | **345.9** | success |
| 4 | i2v + 静音 | GPU1 | **348.9** | success |
| 5 | 首末帧 + 静音 + BGM | GPU0 | 501.4 | success |
| 6 | t2v + 原声 + BGM | GPU1 | 550.4 | success |

worker 分配 `{0: 3, 1: 3}` —— 与上次一致的完美 3:3 均衡。

### 14.2.2 产物验证（ffprobe）

```
case1: h264,video aac,audio 5.167s    (1344x768, t2v base)
case2: h264,video aac,audio 5.167s    (1344x768, t2v+snd→mute)
case3: h264,video aac,audio 5.167s    (1024x768, t2i2v+voice)
case4: h264,video aac,audio 5.167s    (1024x768, i2v+mute)
```

> 注：本次 case3/case4 的产物文件是 **mux 完成后的最终版本**（muxed_*），与原始 `minimax_*.mp4` 在文件名上不同 —— 后台 mux 完成后由 `_muxed` 字典替换产物。脚本下载阶段抓取的是首次返回的 URL（原始），但因为本次调度间隔短，下载时 mux 已经完成，所以最终拿到的是混流版。
>
> 【2026-08-23 修复后】：查询接口已改为先等后处理完成再返回成片 URL（见 14.7 节），不再依赖"下载时恰好 mux 完成"的时序。此处记录的是修复前的测试行为。

---

## 14.3 收益拆解

### 14.3.1 预热带来的提速

```
冷态单任务耗时（首次）：974-991s
热态单任务耗时（稳态）：150-205s
冷启动开销：≈ 800s/卡（~7.5 分钟双卡并行加载）
```

预热把这 800s/卡从业务耗时里**完全剔除**。如果是真实生产场景（用户提交请求时机器刚启），第一次业务请求的 e2e 立刻进入"热态"，不再等待模型加载。

### 14.3.2 后台 MUX 带来的加速

6 个 case 并发完成后，传统实现下后处理是阻塞的：

```
旧：case1 完成 → await mux → case2 完成 → await mux → ... → 串行 6 次
新：case1-6 全部 success 后立即返回，mux 在后台 2 worker 消费
```

实测本次 case3 的 mux 文件 `muxed_1e8f8ea2-...mp4` 在 16:02 创建，比原视频生成时间晚约 5-10 秒。如果客户端对 mux 完成做主动轮询，会感知到这个间隔。

### 14.3.3 整体提速

| 场景 | 旧 | 新 | 加速比 |
|------|----|----|--------|
| 6 任务冷启动并发 | 1344.5s | 550.4s | 2.44× |
| 6 任务热启动并发（理论） | ~1080s（每张卡串行 3 个 ~180s） | ~540s | 2.0× |
| 单任务（业务首次提交） | 974s + mux | **201s + 后台 mux** | **4.8×** |

实际测试从 1344.5s → 550.4s 的 **2.44×** 加速，与"双卡任务级并行"的理论上限（2.0×）相符，并因**消除了冷启动**而略超。

---

## 14.4 启动时长的权衡

| 时点 | 旧实现 | 新实现 |
|------|--------|--------|
| PM2 start | ~5s（API 进程起） | ~5s（API 进程起） |
| /health 返回 ok | ~30s | ~30s（API + worker 都活着） |
| /health 返回 warmed=true | N/A | **~7.5 min 后**（双卡模型加载完） |
| 第一个业务任务可用 | 1344s 内首 case 完成 | **201s**（含业务推理） |

**取舍**：
- 旧实现：服务"在线"≠"可用"，第一个真实任务会被冷启动拖累
- 新实现：服务明确分 `warming` / `ok` 两态，客户端可等待 `ok` 或接受 `warming` 后立即提交

建议客户端行为：
```python
while True:
    h = requests.get(f"{api}/health").json()
    if h["status"] in ("ok", "warming"):  # warming 也可提交，模型已在加载
        break
    time.sleep(5)
```

---

## 14.5 代码层面改动点

```
api_server.py
+ _warmed_workers: set            # warmup 状态
+ _warmup_tasks: dict             # 防重入
+ _post_queue: asyncio.Queue      # 后台 mux 队列
+ _post_workers: list             # 2 个 mux worker 协程
+ _warmup_all_workers()           # 入口
+ _warmup_one(idx)                # 单卡 dummy 任务
+ _mux_worker_loop()              # 后台消费协程
~ startup()                       # 初始化队列 + 启动协程
~ /health()                       # 增加 warming/degraded 三态
~ 两处 await _post_process(...)   # 改为 _post_queue.put_nowait(...)（让 mux 不阻塞轮询返回）
```

净增 89 行，移除 12 行。

---

## 14.6 注意事项与已知限制

1. **业务请求可能撞上冷启**：客户端在 `warming` 期间就提交任务，任务会正常排队，但**第一个推理任务**仍承担模型加载（无法绕过）。要彻底解决需要更激进的"先 warmup 完再 accept connections"
2. **mux 完成事件没有回调**【2026-08-23 已修复】：原先客户端只能靠轮询时观察 `_muxed` 字典是否生成 `muxed_*` 文件，或等待几秒后再次拉取。修复后 `/api/v1/task/{id}` 在需要后处理（静音/口播/BGM）的任务上会先返回 `status: "processing"` + `audio_processing: true`（不返回 videos），成片 `muted_/muxed_*.mp4` 落盘后才返回 `success` + 成片 URL + `audio_processed: true`——客户端只需继续轮询到 `success`，不再"拿到 URL 却可能是原片"
3. **warmup 任务有副作用**：会留下 `minimax_warmup_*.mp4` 在 `ComfyUI/output/`，需要定期清理或重写 `build_prompt` 让 warmup 不产生输出文件
4. **mux 失败现在会暴露给客户端**：`_post_process` ffmpeg 失败后任务查询返回 `error` / `failed`（错误为 `audio post-process failed`），OpenAI 兼容口失败结构为 `{"error": {"message": "..."}}`，不再静默给原片
5. **重启后成片索引自动重建**：`_load_state()` 会调用 `_index_muxed_dir()` 扫描 `muxed/`，服务重启后历史任务的 `muted_/muxed_*.mp4` 仍可查询/下载（`/api/v1/video/{filename}` 也会兜底伺服磁盘上未登记的成片文件）

---

## 14.7 2026-08-23 修复：查询返回路径与后台 MUX 的衔接

文档 14.2.2 描述的"下载时 mux 已经完成，最终拿到混流版"在修复后成为**确定行为**，而非碰运气。

修复引入的查询状态机（详见《11.HTTP接口测试过程文档》11.13 节）：

| 查询时后处理状态 | `/api/v1/task/{id}` 返回 |
|------|------|
| 不需要后处理 | `success` + 原片 URL |
| 需要后处理但未完成 | `processing` + `audio_processing: true`，无 videos |
| `muted_/muxed_*.mp4` 就绪 | `success` + 成片 URL + `audio_processed: true` |
| ffmpeg 失败 | `error`（`audio post-process failed`） |

新增函数：`_lookup_processed` / `_output_phase` / `_resolve_result_videos` / `_enqueue_post` / `_index_muxed_dir`。后台队列逻辑（`_post_queue` + 2 个 `_mux_worker_loop`）不变，只是查询端改为先查成片、pending 时入队并继续等。

---

## 14.8 复现命令

```bash
# 1. 启动服务（注意 warmup 期 ~7.5 min）
pm2 restart minimax-h3-api

# 2. 等 warmup 完成
while true; do
  s=$(curl -s http://127.0.0.1:8000/health | python3 -c "import json,sys; print(json.load(sys.stdin)['status'])")
  echo "status: $s"
  [ "$s" = "ok" ] && break
  sleep 30
done

# 3. 运行并发测试
cd /home/ubuntu/comfy
./venv/bin/python playground/bench_6cases_concurrent.py \
  --api http://127.0.0.1:8000 \
  --steps 10 \
  --download \
  --out-dir /home/ubuntu/comfy/output/step_10_v2

# 4. 观察输出
#    - 提交阶段: 0.1s 左右（_pick_lock 改造有效）
#    - worker 分配: 3:3 均衡
#    - Case 1/2 完成: ~200s
#    - 全部完成: ~550s
#    - 静音/口播/BGM 任务：轮询到 processing（audio_processing: true）
#      → 成片落盘后 success + muted_/muxed_* URL，不会拿到原片
```

---

## 14.7 2026-08-23 修复：查询返回路径与后台 MUX 的衔接

文档 14.2.2 描述的"下载时 mux 已经完成，最终拿到混流版"在修复后成为**确定行为**，而非碰运气。

修复引入的查询状态机（详见《11.HTTP接口测试过程文档》11.13 节）：

| 查询时后处理状态 | `/api/v1/task/{id}` 返回 |
|------|------|
| 不需要后处理 | `success` + 原片 URL |
| 需要后处理但未完成 | `processing` + `audio_processing: true`，无 videos |
| `muted_/muxed_*.mp4` 就绪 | `success` + 成片 URL + `audio_processed: true` |
| ffmpeg 失败 | `error`（`audio post-process failed`） |

新增函数：`_lookup_processed` / `_output_phase` / `_resolve_result_videos` / `_enqueue_post` / `_index_muxed_dir`。后台队列逻辑（`_post_queue` + 2 个 `_mux_worker_loop`）不变，只是查询端改为先查成片、pending 时入队并继续等。

---

## 14.8 复现命令

```bash
# 1. 启动服务（注意 warmup 期 ~7.5 min）
pm2 restart minimax-h3-api

# 2. 等 warmup 完成
while true; do
  s=$(curl -s http://127.0.0.1:8000/health | python3 -c "import json,sys; print(json.load(sys.stdin)['status'])")
  echo "status: $s"
  [ "$s" = "ok" ] && break
  sleep 30
done

# 3. 运行并发测试
cd /home/ubuntu/comfy
./venv/bin/python playground/bench_6cases_concurrent.py \
  --api http://127.0.0.1:8000 \
  --steps 10 \
  --download \
  --out-dir /home/ubuntu/comfy/output/step_10_v2

# 4. 观察输出
#    - 提交阶段: 0.1s 左右（_pick_lock 改造有效）
#    - worker 分配: 3:3 均衡
#    - Case 1/2 完成: ~200s
#    - 全部完成: ~550s
```